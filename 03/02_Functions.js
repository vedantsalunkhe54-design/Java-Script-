function calculateprice(...num){ // This is the rest used to add multiple values
    return num;
}

console.log(calculateprice(34,45,21,45,634,3461));

const user={
    name:"Vedant",
    age: 20,
    interest: "JavaScript"
}

function uvalue(anyobject){
    console.log(`The user name is ${anyobject.name} and his age is ${anyobject.age}`);
}
uvalue(user);

const newarr=[200,400,600]

function returnarr(mynewarr){
    return mynewarr[1];
}
console.log(returnarr(newarr));
